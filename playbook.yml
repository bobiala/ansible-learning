-
  name: Internal servers automation
  hosts: internals
  tasks:
    - name: Install dependencies
      apt: name={{ item }} state=present
      with_items:
       - python
       - python-setuptools
       - python-dev
       - build-essential
       - python-pip

    - include: tasks/fail2ban.yaml
